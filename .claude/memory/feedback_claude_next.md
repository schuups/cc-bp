---
name: feedback-claude-next
description: CLAUDE_NEXT.md is a protected working file — never edit it without explicit user permission
metadata:
  type: feedback
---

`.claude/CLAUDE_NEXT.md` is the gradual refactoring target for `.claude/CLAUDE.md`. Content is ported there section by section during the blueprint refactoring, eventually replacing CLAUDE.md entirely.

**Never update `CLAUDE_NEXT.md` without explicit user permission.** Every change requires the user to request it first.

**Why:** The user explicitly established this rule to maintain control over what enters the refactored blueprint.

**How to apply:** If you think something should go into `CLAUDE_NEXT.md`, say so and wait for the user to approve before touching the file.
