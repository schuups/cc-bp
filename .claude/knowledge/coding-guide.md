# Coding Guide

> **Purpose:** Language- and framework-specific rules for this project. Captures conventions, non-obvious patterns, and explicit prohibitions that apply to every line of code written here. This file is not a general style guide — it contains only rules that are specific to this project or that override common defaults.
>
> **Producers:** `/onboard` generates the initial guide based on detected language(s) and framework conventions; updated as new conventions are established or anti-patterns are discovered in review.
>
> **Consumers:** `implementer` (reads before writing any code), `auditor` (verifies code style compliance), `/ccbp:checkpoint` Check 2 (lint and type check configuration reference).
>
> **Section conventions:**
> - **Style** — formatting, naming, file organisation rules; prefer concrete examples over prose descriptions
> - **Testing** — required test types, coverage expectations, naming conventions for test files and cases
> - **Linting** — which linter(s) and type-checker(s) are configured; the exact commands to run; zero-tolerance vs. warning-level rules
> - **Patterns** — approaches that are preferred or required in this codebase; explain *why* when the reason is non-obvious
> - **Anti-patterns** — approaches that are explicitly prohibited; always state the reason and the preferred alternative

<!-- Generated by /onboard based on detected language(s). -->

## Language: [detected by /onboard]

## Style

## Testing

## Linting

## Patterns

## Anti-patterns
